<%= form_for(@statut) do |f| %>
  <% if @statut.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@statut.errors.count, "error") %> prohibited this statut from being saved:</h2>

      <ul>
      <% @statut.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :intitule %><br />
    <%= f.text_field :intitule %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
